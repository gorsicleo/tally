---
"tally": patch
---

[severity:minor] Improves locked-app startup flow by automatically attempting device authentication when available and falling back to PIN entry only if device auth fails, reducing unlock friction while preserving secure fallback behavior.
